interface IProps {
  isHollow?: boolean,
  hasArrow?: boolean,
  basis?: string,
  text?: string,
  url?: string,
  isOuterRef?: boolean,
  isForHeader?: boolean,
  buttonType?: 'button' | 'submit' | 'reset',
}

export default IProps;
